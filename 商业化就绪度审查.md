# AI视频Agent平台 · 商业化就绪度审查

审查时间：2026-08-05　　审查范围：全部代码、skill 资产、数据层、Web 层

---

## 结论先说

**当前状态：单人自用的生产工具，已经可以真实干活。**
**距离商业化（对外提供服务、多人使用、收费）还差一段，缺的不是功能，是运营基础设施。**

功能层面七个里程碑全做完了，逻辑闭环、有执法、有测试。
但"能用"和"能卖"之间隔着鉴权、多租户、可观测、备份这几样东西，
它们一个都还没有。

下面按严重程度分级。**P0 是不做就不能对外的**，P1 是上线后很快会咬人的，
P2 是可以带着上线但要有计划的。

---

## 本次审查已修复的问题

| 问题 | 风险 | 已做的处理 |
|---|---|---|
| **绝对路径逃逸** | 产物路径若被污染可读到 `/etc/passwd` 或 `config/secrets.env` | `ArtifactStore.resolve()` 强制解析结果落在 `projects/` 内，越界抛 PermissionError |
| **Skill 目录穿越** | `/skills/../..` 可能读到仓库外 | Web 层 `_load_skill()` 校验路径前缀 |
| **产物非原子写入** | 写到一半崩溃留下截断 JSON，产物是核心资产，损坏就得重跑整个节点 | 改为先写 `.tmp` 再 rename |
| **SQLite 并发未配置** | 工作台与命令行同时操作会撞 `database is locked` | 开启 WAL + busy_timeout 15s |
| **空 diff 落盘** | 零字节文件被统计成"改过一次"，污染飞轮的高频字段统计 | `record_feedback` 拒绝空 diff，解析器跳过损坏文件 |
| **零测试** | 改任何一处都不知道有没有弄坏别的 | 新增 29 个回归测试，接入 `avctl smoke` |

---

## P0 · 不解决就不能对外

### 1. Web 层鉴权（已做：可选口令 + 非本机强制）

登录页 + session + 统一前置钩子已就位（`web/security.py`、`web/views/auth.py`）。

默认**不开**：单人本机用加登录纯属添堵。在「系统 → 密钥」页设口令后生效，
口令用 pbkdf2-sha256（24 万轮）存 `config/auth.json`，不落明文。

关键是那条强制：`avctl web --host` 绑到非 localhost 且没设口令时**拒绝启动**。
原先那里只打印一行警告然后照常暴露 —— 警告拦不住事故。

仍未做：HTTPS。工作台是明文 HTTP，只适合本机或可信内网，别直接上公网。

### 2. CSRF 防护（已做）

所有写操作都验 token（`web/security.py`），原生表单走 hidden 字段、
HTMX 走 `X-CSRFToken` 头，两条路都通；会话 cookie 加了 HttpOnly + SameSite=Lax。

这一项**本机自用也必须做**，和是否对外无关：工作台跑在 127.0.0.1，
但同源策略拦不住跨站表单提交 —— 你在别的标签页打开的任何网页都能悄悄
POST 到 localhost:5001，替你盖章、合入 skill 建议、清掉 API 密钥，
或者触发一串烧钱的模型调用，而你什么都看不见。
代价是零（用户感知不到），所以默认常开。

### 3. 单租户设计，没有数据隔离

数据库里没有 `user_id` 这一层。`accounts` 表是"公众号账号"的意思，
不是"平台用户"。要卖给别人用，整个 schema 需要加一层租户，
产物目录也要按租户隔离。**这是改动最大的一项，越晚做越贵。**

如果商业化方向是 SaaS，这件事应该在有第二个用户之前做完。
如果方向是"卖工具让人自己部署"，可以不做。**这个方向要先想清楚。**

### 4. API 密钥明文存放

密钥现在能在网页「密钥设置」页填，存进 `config/secrets.json`（明文，靠
`.gitignore` + 文件权限 0600 保护），环境变量注入仍然可用且优先级更高。
单人本机用可以接受，多人或者部署到服务器上就不行了——
那种场景最低要求是走真正的密钥管理服务，密钥不落盘在应用目录里。

---

## P1 · 上线后很快会咬人

### 5. 无限增长，没有任何清理

- `feedback/` 每次人工修改写一个文件，永不清理
- `versions/` 每次改 SKILL.md 存一版，永不清理
- `config/calls.jsonl` 每次模型调用追加一行，永不轮转
- `projects/` 产物只增不减

单人用一年可能没事，但没有任何上限意味着某天会突然爆掉磁盘。
应该给 versions 保留最近 N 版、calls.jsonl 做轮转、
feedback 在合入建议后归档。

### 6. 模型调用没有成本护栏

`ModelGateway` 记了 token 日志，但没有任何预算限制。
一个写错的循环可以在几分钟内烧掉大量费用。
应该有：单次调用上限、单日预算上限、超限拒绝而非继续。

### 7. 没有任务队列，长调用会卡住界面

工作台目前只做展示和审批，真正的生成还得回命令行。
一旦把 `run` 搬进界面，一次模型调用几十秒会直接卡死请求。
需要后台任务（哪怕是最简单的线程池 + 状态轮询）。

### 8. 错误处理有三处裸 `except Exception`

`gateway/client.py`、`flywheel.py`、`web/app.py` 各一处。
现在是有意为之的兜底，但生产环境里它们会吞掉真实错误，
让问题变成"没反应"而不是"报错"。至少要记日志。

### 9. 没有备份策略

SQLite 单文件 + 文件系统产物，没有任何备份。
误删一次 `projects/` 就全没了。至少要有定时打包到别处。

---

## P2 · 可以带着上线，但要有计划

### 10. references 检索还是全量带入

现在按标签过滤后全文注入。references 库上千条之后会撑爆上下文预算。
`refkit/` 目录建了但还是空的，裁剪逻辑没实现。
到那个量级需要上向量检索。

### 11. 提示词质量没有人工快速扫读的视图

写测试用例时我自己拼出过 `Absolutely no人物 removed` 这种中英混杂残句，
校验器完全没管——它只检查结构，不检查提示词内容质量。
工作台应该有一个专门的提示词预览页，让人扫一眼就能发现这类低级错误。

### 12. 三项人工验收从未做过

- **M1**：剧本内容质量（跑真模型，读产出）
- **M2**：角色一致性（角色板提示词去图像模型跑）
- **M3**：导演风格可分辨性（盲测）

这三项是代码验不了的，而且 **M2 那一关最贵**——
锚定描述的方法论如果有问题，后面拍多少集返工多少集。
商业化之前必须过一遍。

### 13. 图像/视频模型未直连

现在靠手动复制粘贴提示词。自用可以忍，
但如果要对外提供服务，这一步必须自动化，否则交付流程不成立。

### 14. 没有操作审计

谁在什么时候盖了章、合入了哪条建议、改了哪个 SKILL.md——
`skill_versions` 表记了一部分，但不完整，也没有操作人。
多人使用时这是必需品。

---

## 架构层面的评价

这部分我认为是健康的，值得保持：

**契约先行是对的。** 五份契约定死之后，五个 skill 可以独立开发独立验收，
出问题能精确定位到某一层。开发过程中挖出的三个 bug
（空壳 skill 过审、分镜缺上游照跑、角色板漏人不报错）
都是靠契约执法暴露出来的。

**导演作为注入层而非节点是对的。** 同一个 skill 在不同导演下产出不同，
skill 本身保持通用。如果做成节点，风格就只能在某个环节生效。

**题材做成模块而非拆多个 skill 是对的。** 拆成三个作家 skill 的话，
方法论层会有三份拷贝，改一条红线要改三遍，半年后会漂移成三个不同的东西。

**飞轮保留人工批准是对的。** skill 是这个平台唯一的资产，
全自动合入一次出错会污染之后所有产出，且很难回溯到"哪一版开始变坏"。
合入后自动复检规范并回滚这一层保护，实测有效。

---

## 建议的推进顺序

**第一步（对外之前必须做完）**：鉴权 + CSRF + 密钥不落盘。
这三件加起来不到一天，但不做就不能碰公网。

**第二步（想清楚再动）**：确定商业化形态——SaaS 还是卖自部署。
决定了才知道要不要做多租户。多租户越晚做越贵，
但如果方向是自部署，做了就是白做。

**第三步（并行）**：把三项人工验收跑完。
尤其 M2 一致性，这一关不过，后面所有产能都是负资产。

**第四步**：成本护栏 + 清理策略 + 备份。这三样是"运营"的最低配置。

**第五步**：任务队列 + 图像视频模型直连。这两样决定了能不能规模化交付。
