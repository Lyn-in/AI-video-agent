# 测试指南

本文里的每条命令我都实跑验证过。按顺序做，遇到不一致就是 bug，直接告诉我。

---

## 先说清楚：哪些做完了，哪些没有

**做完了**：规划里的七个里程碑（M0-M6）。五个工种 skill、三位导演、
五个题材模块、拉片员、流水线引擎、审核门、工作台界面、质量飞轮、29 个回归测试。

**没做**：审查报告里的 P0/P1 项，它们本身也是开发工作——

- Web 层鉴权、CSRF 防护（**所以现在只能在本机跑，不要暴露到公网**）
- 密钥不落盘
- 多租户隔离（取决于你选 SaaS 还是卖自部署，方向没定就不该动）
- 图像/视频模型直连（现在还是手动复制提示词到网页端）
- 成本护栏、清理策略、备份

---

## 第一步：装起来，跑自检（5 分钟）

```bash
tar -xzf av-agent-v1.0.tar.gz
cd av-agent
pip install flask --break-system-packages     # 只有工作台需要
python3 cli/avctl.py smoke
```

**预期**：最后一行 `✓ 自动验收通过`，中间有 `Ran 29 tests ... OK`。

这一步全绿，说明代码层面是好的。**但它验不了内容质量**——
下面第四步开始才是真正的验收。

```bash
python3 cli/avctl.py init
```

---

## 第二步：不花钱先摸一遍（10 分钟）

所有命令加 `--dry-run` 会用占位产物跑，不调用模型、不花钱。
目的是熟悉流程、确认环境没问题。

```bash
# 起一个剧集，跑第一个节点
python3 cli/avctl.py run writer --brief "测试" --series demo --episode 1 \
    --genre 爽剧 --dry-run

# 看流水线状态
python3 cli/avctl.py pipeline --series demo --episode 1
```

**预期**：看板显示 N1-N2 待审，N3-N6 未开始，卡点指向 N1-N2。

```bash
# 剩下四个节点。注意 N4/N5 不带 --episode（角色板场景板是剧集级，全剧共享）
D=projects/default/s1/demo

python3 cli/avctl.py run playwright --input $D/ep01/story_file.json \
    --series demo --episode 1 --director jiazhangke --dry-run

python3 cli/avctl.py run casting     --input $D/ep01/screenplay.json \
    --series demo --director jiazhangke --dry-run

python3 cli/avctl.py run set_dresser --input $D/ep01/screenplay.json \
    --series demo --director jiazhangke --dry-run

python3 cli/avctl.py run storyboard  --input $D/ep01/screenplay.json \
    $D/_series/character_board.json $D/_series/location_board.json \
    --series demo --episode 1 --director jiazhangke --dry-run
```

**顺手验两条执法**（这两条是平台的核心保护，值得亲眼看一次）：

```bash
# 分镜缺角色板/场景板 → 应当拒绝执行
python3 cli/avctl.py run storyboard --input $D/ep01/screenplay.json \
    --series demo --episode 1 --dry-run
```
预期：`✗ 缺少必需的上游产物，拒绝执行`，并列出缺哪两份、由谁产出。

```bash
# 看导演匹配器
python3 cli/avctl.py director match --genre 武侠 打脸
```
预期：邵氏排第一，另两位 0 分。

---

## 第三步：打开工作台（5 分钟）

```bash
cd av-agent                       # 必须在项目根目录下运行
pip install flask --break-system-packages    # 只需装一次
python3 cli/avctl.py web
```

看到 `✓ 生产工作台: http://127.0.0.1:5001` 之后，
浏览器打开 **http://127.0.0.1:5001**。Ctrl+C 停止。

**常用参数**：

```bash
python3 cli/avctl.py web --port 8080     # 换端口（5001 被占用时）
python3 cli/avctl.py web --debug         # 排查问题时开，平时别开
```

**关于 `--host`**：默认只监听 127.0.0.1，也就是只有本机能访问。
改成 `0.0.0.0` 会让同一网络的其他设备也能打开——**工作台目前没有任何
鉴权和 CSRF 防护，任何能访问到它的人都能改 SKILL.md、盖章、合入建议**。
命令会先弹警告再启动。只在可信内网这么做，绝不要暴露到公网。

**第一次打开是空的**，首页会提示"还没有剧集"。这是正常的——
产物要先用命令行跑出来才会出现。Skill 管理和导演库不依赖数据，可以直接看。

**要看的四件事**：

1. 首页能看到 demo 剧集
2. 点进去，流水线看板五个节点——**红章是通过，琥珀是当前卡点，灰的是还没轮到**
3. 点任一节点进产物页——顺序是 引用申报 → 审阅版 → 盖章/打回 → 直接修改
4. 在「直接修改」里故意删掉一个必填字段保存 → **应当拒绝并列出具体字段路径**

第 4 条是重点：界面上的人工修改同样过契约校验，不合规不落盘。

---

## 第四步：真跑一次（这里开始花钱）

密钥推荐在网页「密钥设置」页填（支持多厂商，参见 `core/gateway/client.py`
的 `PROVIDERS`）。命令行测试环境也可以用环境变量，优先级更高：

```bash
cp config/secrets.env.example config/secrets.env
# 编辑填入 ANTHROPIC_API_KEY
source config/secrets.env
```

然后把第二步的命令去掉 `--dry-run` 重跑一遍，`--brief` 换成你真正想拍的东西。

**建议第一次只跑到 N3（故事+剧本）就停下来读。** 内容不行的话，
后面四个节点跑了也是白跑。

---

## 三项人工验收（代码验不了，只能你来）

### 验收一：内容质量（M1）

读产出的故事档案和剧本，判断**这个剧本值不值得往下做**。

建议至少验两个差异大的题材（比如爽剧和现实），只验一个分不清是
方法论好还是恰好那个题材模块写得准。

包里 `_testcase/` 有一份我按同一份 SKILL.md 手写的完整产出（现实+催泪，
180 秒，《认领》），可以先看那个判断水准，觉得不行直接说哪里不对。

**不过关怎么办**：改 `skills/writer/SKILL.md` 的方法论或质量红线，
或者改对应的题材模块，重跑对比。工作台的 skill 详情页可以在线改，
保存自动存档旧版。

### 验收二：一致性（M2）——**这一关最要紧**

```bash
# 从角色板里取提示词
cat $D/_series/character_board.json | python3 -m json.tool | grep -A2 '"en"'
```

拿同一个角色的五条英文提示词，去你常用的图像模型各跑一张，
看**五张图是不是同一个人**。

然后拿分镜表的 `video_prompt.en` 去视频模型跑，看画面里的人和景
跟角色板场景板对不对得上。

**为什么这一关最要紧**：一致性是整条流水线最贵的返工点。
锚定描述的方法论如果有问题，后面拍多少集就返工多少集。

**不过关怎么办**：改 `skills/casting/SKILL.md` 的锚定维度或写法要求。
可能要针对你实际用的图像模型调整提示词的组织方式——
不同模型吃的方言不一样。

### 验收三：导演风格可分辨（M3）

```bash
python3 cli/avctl.py blindtest playwright \
    --directors jiazhangke wongkarwai \
    --input $D/ep01/story_file.json --out blindtest
```

生成两份**去标识、随机打乱**的剧本，答案封在 `blindtest/_答案.txt`。
读完两份再打开答案。

**分辨得出** → 风格注入生效，M3 通过。
**分辨不出** → 注入机制无效，应当停下来重新设计，而不是继续往上加功能。

挑贾樟柯和王家卫这一对，因为它俩擅长题材有重叠（都吃情感、现实）
但风格南辕北辙，是最严的条件。拿邵氏跟另两位比等于送分。

---

## 第五步：让飞轮转起来（长期）

飞轮的原料是你在审核门里的人工修改。每次「保存修改」，
diff 会自动归集到对应 skill 的 `feedback/`。

```bash
python3 cli/avctl.py flywheel status              # 看攒了多少
python3 cli/avctl.py flywheel analyze writer      # 修改模式分析
python3 cli/avctl.py flywheel suggest writer      # 攒够 5 条才给生成建议
python3 cli/avctl.py flywheel review writer       # 看建议
python3 cli/avctl.py flywheel apply writer --pick 1 3    # 合入选中项
```

**注意 analyze 输出里的「按导演」分布**：如果修改集中在某一个导演下，
问题可能出在导演约束而不是 skill 本体，那时候改 skill 会越改越糟。

合入有三道保护，实测有效：精确匹配原文、原文不唯一时拒绝、
合入后复检规范不合规自动回滚。

---

## 出问题怎么给我反馈

按这个顺序说，我能最快定位：

1. **哪一步**（第几步、哪条命令）
2. **预期什么、实际什么**（贴报错原文）
3. 如果是内容质量问题，**贴产物 + 说哪里不对**，不用说该怎么改，
   我来判断是改方法论、改题材模块还是改导演约束

内容质量的反馈尤其重要——代码问题我自己能测出来，
内容好不好只有你能判断。
