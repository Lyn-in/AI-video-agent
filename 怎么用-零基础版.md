# 怎么用（零基础版）

**不需要打任何命令。双击一个文件就行。**

---

## 第一次用

### 1. 解压

把 `av-agent-v1.1.tar.gz` 解压出来，得到一个 `av-agent` 文件夹。
放在你找得到的地方，比如桌面。

### 2. 双击启动文件

进到 `av-agent` 文件夹，里面有两个启动文件，按你的电脑选一个：

- **Mac** → 双击 **启动.command**
- **Windows** → 双击 **启动.bat**

会跳出一个黑色窗口，自己开始跑。

> **Mac 第一次双击可能提示"无法打开，因为来自身份不明的开发者"**
> 解决：右键点这个文件 → 选「打开」→ 再点「打开」。只需做这一次。

### 3. 跟着窗口提示走

窗口会自己做完这些事：检查环境、装组件、建数据库。

中间会停一次，问你要 **API 密钥**：

```
还没有填 API 密钥。
去 console.anthropic.com 创建一把，复制过来粘贴在下面。
密钥:
```

去 console.anthropic.com 注册账号，在 API Keys 页面创建一把，
复制过来粘贴进去，按回车。

**这一步做完就永久记住了，以后不用再填。**

（暂时没有密钥也可以直接按回车跳过，能进去看界面，但不能生成内容。）

### 4. 浏览器自动打开

窗口最后会显示：

```
网页地址: http://127.0.0.1:5001
★ 这个窗口不要关，关了网页就打不开了。
```

浏览器会自己弹出来。没弹的话，手动复制那个地址到浏览器。

---

## 以后每次用

**双击同一个启动文件就行。** 不用再填密钥、不用装东西，几秒钟就开。

用完了，在黑窗口里按 **Ctrl + C**，或者直接关掉窗口。
做好的东西都存着，不会丢。

---

## 网页上怎么做一部片子

### 1. 首页填名字

填个剧集名字，比如"认领"。题材随便填，比如"现实, 催泪"。
点「建好，开始做」。

### 2. 五个步骤，一步一步来

进去会看到五格：

```
N2 故事档案   ← 从这里开始
N3 剧本
N4 角色板
N5 场景板
N6 分镜表
```

在 N2 那一格的输入框里，写一句话说你想拍什么，比如：

> 父子俩在同一所学校，一个念书一个做保洁，约好互不相认

点「生成这一步」。

### 3. 等它跑完

几十秒，页面会自己刷新。跑完那格显示"待审"。

### 4. 点进去读

觉得好 → 点「盖章通过」
觉得不行 → 在最下面写清楚哪里不对 → 点「打回重做」 → 回去重新生成

### 5. 通过之后做下一格

一直做到 N6。每一格都是同样的三步：**生成 → 读 → 通过或打回**。

### 6. 最后拿提示词去出图出片

N6 分镜表通过之后点进去，里面是一条条提示词。
复制出来，粘贴到你用的出图工具、出视频工具里。

**这一步暂时还要手动复制，没做自动对接。**

---

## 遇到问题

**双击没反应 / 闪一下就关了**
→ 多半是没装 Python。去 python.org 下载安装。
**Windows 安装时务必勾选 "Add Python to PATH"**，漏勾了就是这个症状。
装完重新双击。

**窗口里说"组件安装失败"**
→ 网络问题。挂上梯子或者换个网络重试。

**页面上没有「生成这一步」按钮**
→ 密钥没填。关掉窗口，删掉 `config` 文件夹里的 `secrets.env`，
重新双击启动，会再问你要一次。

**点了生成，那格显示"这一步没成功"**
→ 下面会写原因。最常见两种：
密钥不对（去 console.anthropic.com 确认），
或者写着"缺少上游"（前面某步还没通过，先把前面做完）。

**关了黑窗口，网页打不开了**
→ 正常。网页是靠那个窗口撑着的，重新双击启动就行。

**换台电脑用**
→ 整个 `av-agent` 文件夹拷过去，所有东西都在里面。

---

## 一件重要的事

**这个网页只有你自己的电脑能打开，它没有登录密码。**

网上可能有教程教你改配置让别人也能访问 —— **别改**。
现在谁能访问到，谁就能改你的所有东西。
要给别人用，得先加登录功能，那部分还没做。

---

## 把 skill 拿到 Claude Code / Codex 里单独用

平台真正的资产是那套 skill——方法论、判断标准、禁令、题材模块、导演约束。
平台只是把它们编排起来的壳子。所以 skill 可以单独拿出去用。

命令行跑一次（这个必须用命令行，但只有一条）：

```
python3 cli/avctl.py export
```

会在项目下生成 `export/` 文件夹，里面三个版本：

### `export/claude/` —— Claude Code 用

把里面的各个文件夹复制到你项目的 `.claude/skills/` 下面：

```
你的项目/
└── .claude/
    └── skills/
        ├── writer/          （作家）
        │   ├── SKILL.md
        │   └── genres/      五个题材模块
        ├── playwright/      （编剧，带题材+导演）
        ├── casting/         （选角，带导演）
        ├── set_dresser/     （场务，带导演）
        ├── storyboard/      （分镜，带导演）
        └── film_analyst/    （拉片员）
```

装好之后在 Claude Code 里直接说「帮我想个短剧故事」「把这个故事写成剧本」，
对应的 skill 会自己启用。

### `export/codex/` —— Codex 或其他 agent 工具用

每个 skill 是一个自包含的单文件，题材模块和导演约束都内嵌在里面了。
另有一份 `AGENTS.md` 说明整条流水线怎么走。

### `export/plain/` —— 任何对话框

纯 Markdown，复制粘贴到任何 AI 对话框里都能用。

---

**导出版和平台版的区别**（这个要知道）：

平台版有**契约校验器强制执法**——编剧写了镜头会被直接打回、
角色板漏了人物会当场报错、分镜引用不存在的角色会被拦截。

导出版只有文字约定，没有强制。模型多数时候会遵守，但没人兜底。

另外导出版没有审核门、没有版本管理、没有质量飞轮。

**所以：想快速用、想在别的工具里用，就用导出版；
要严格执法、要积累和迭代，还是用平台。**
